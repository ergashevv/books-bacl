services:
  - type: web
    name: books-backend
    env: node
    plan: standard
    buildCommand: cd server && NODE_OPTIONS='--max-old-space-size=1500 --no-warnings' npm install --no-optional --no-audit --no-fund --prefer-offline --legacy-peer-deps --ignore-scripts && NODE_OPTIONS='--max-old-space-size=1500 --no-warnings' npm run build && NODE_OPTIONS='--max-old-space-size=1500 --no-warnings' npm install --production --no-optional --no-audit --no-fund --prefer-offline --legacy-peer-deps --ignore-scripts
    startCommand: cd server && npm start
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
      - key: NODE_OPTIONS
        value: --max-old-space-size=1500 --expose-gc --no-warnings
      - key: DATABASE_URL
        fromDatabase:
          name: books_db # Agar Render-da DB bo'lsa, aks holda qo'lda kiriting
      - key: BOT_TOKEN
        sync: false # Render dashboard'dan qo'lda qo'shing
